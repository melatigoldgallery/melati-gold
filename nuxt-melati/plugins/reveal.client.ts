// Reveal plugin deprecated - using AOS (Animate On Scroll) instead
// This plugin is kept for reference but disabled to avoid animation conflicts

export default defineNuxtPlugin(() => {
  // AOS plugin now handles all scroll-triggered animations
  // See plugins/aos.client.ts for configuration
});
